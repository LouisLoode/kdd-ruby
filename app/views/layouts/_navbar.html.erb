<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
	<!-- Brand and toggle get grouped for better mobile display -->
	<div class="navbar-header">
		<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-brand-centered">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
		</button>
		<div id="logo" lass="navbar-brand navbar-brand-centered">
			<%= link_to image_tag("/assets/logomini.svg"), root_path %>
		</div>
	</div>
	<!-- Collect the nav links, forms, and other content for toggling -->
	<div class="collapse navbar-collapse" id="navbar-brand-centered">
		<% if user_signed_in? %>
		<form type="GET" action="<%= search_path %>" class="navbar-form navbar-left search-form" role="search">
			<div id="search" class="input-group">
				<input type="text" id="query" name="query" class="form-control" placeholder="Rechercher" />
				<div class="input-group-btn">
					<button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
				</div>
			</div>
		</form>
		<% end %>
		<ul class="nav navbar-nav navbar-right">
			<% if user_signed_in? %>
					<li id="user_menu" class="dropdown user_menu">
							<div class="cont_img">
								<%= link_to image_tag(current_user.avatar.present? ? current_user.avatar : '/assets/Avatar_default.png'), show_profile_path %>
							</div>

							<a id="linkname" href="#" class="namelink" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">@<%= current_user.name %>
							<span class="caret"></span>
							</a>

							<ul id="dropdown-menu" class="dropdown-menu">
								<li class="linkdrop"><%= link_to 'Voir mon profil', show_profile_path %></li>
								<li class="linkdrop"><%= link_to 'Modifier mon profil', edit_user_registration_path %></li>
								<li class="divider"></li>
								<li class="signout"><%= link_to "Déconnexion", destroy_user_session_path, :method => :get %></li>
							</ul>

					</li>

				<% else %>
					<li id="login"><%= link_to "Connexion", new_user_session_path %></li>
				<% end %>
			</ul>
		</div><!-- /.navbar-collapse -->
	</nav>
