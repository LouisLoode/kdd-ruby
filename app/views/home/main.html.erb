

<div id="part" class="mainmargin container gover">
    <div id="fixprofil" class="row">

        <div class="col-xs-12 col-sm-2 col-md-3">
           <div class="listecategories">

           </div>
        </div>

        <div class="col-md-9">
            <div class="row">
                <div class="col-md-7">
					<% if @feed_items.size > 0 %>
					  <% @feed_items.each do |post| %>
					    <%= render partial: 'posts/card', locals: {post: post, user: @user} %>
					  <% end %>
					<% else %>
					<p class="text-center">Vous ne suivez personne pour l'instant ou bien vous n'avez encore rien publié</p>
					<% end %>
                </div>
                <div class="col-md-5">
                  <%= render partial: 'posts/form'%>
                </div>
            </div>
        </div>
    </div>
</div>